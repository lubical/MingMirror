# 贡献指南

感谢你愿意为明镜贡献力量。明镜是一个熔炼九大思想声部的精神导师 AI——你的贡献会让她在面对不同困境时更准确、更有深度。

本文档告诉你**怎么贡献**、**有哪些约束**，以及**提交前必做的检查**。

---

## 我能贡献什么

| 类型 | 说明 | 难度 |
|---|---|---|
| **新语料** | 九声部的新金句条目（道德经/论语/六祖坛经/资本论等经典中未被收录的） | 中 |
| **改进白话解析** | 让某条语料的白话更贴近现代生活、更易懂 | 低 |
| **新概念** | 跨声部的概念条目，链向关联语料 | 中 |
| **方法步骤** | 改进某个方法的操作步骤（更具体、更可执行） | 中 |
| **声部导读** | 补充某声部的常见误读、回避边界 | 高 |
| **对话示例** | 新增某个困境子场景的完整对话示范 | 中 |
| **CLI 改进** | bug 修复、检索质量提升、新功能 | 高 |
| **试金石困境** | 新的测试案例（用于实测明镜表现） | 低 |

---

## 贡献前的必读

动手前请先读这两份，避免重复或冲突：

1. **[设计文档](docs/superpowers/specs/2026-08-07-spiritual-mentor-design.md)**——权威源。尤其：
   - §4 解药矩阵（五类困境 × 首选/辅选/回避声部）
   - §8 知识资产规范（语料/概念/方法/导读/示例的字段结构）
   - §12 实测修订记录（改动前先读，避免重蹈覆辙）
2. **[AGENTS.md](AGENTS.md)**——项目约定（声部白名单、困境白名单、安全条款、矩阵一致性）

---

## 语料贡献规范

### 七字段结构（必须完整）

每条语料是一个 JSON 对象，加入对应声部的文件（如 `corpus/laozi.json`）：

```json
{
  "id": "laozi-052",
  "原文": "千里之行，始于足下。",
  "出处": "《道德经》第六十四章",
  "白话解析": "再远的路也是从脚下开始的。别被目标的庞大吓住——今天走一步，就算一步。",
  "适用场景": ["焦虑与精神内耗", "意义与方向危机"],
  "禁用场景": ["真实资源危机（失业/破产/疾病）——先处理现实，不急着谈知足"],
  "关联声部": "laozi"
}
```

**字段约束**：

| 字段 | 要求 |
|---|---|
| `id` | 格式 `<声部>-<序号>`，全局唯一，序号接续该声部现有最大值 |
| `原文` | 必须准确，宁化用大意不编造原文；化用须在 `白话解析` 注明 |
| `出处` | 必须可核实（篇名/章节） |
| `白话解析` | 现代生活化转译，避免学究气；点出"对什么困境有用" |
| `适用场景` | 数组，取值必须来自困境白名单（见下） |
| `禁用场景` | **必填**——这是防"开错药"的最后一道闸。说明该语料在什么情况下**不该用** |
| `关联声部` | 取值必须来自声部白名单（见下） |

### 白名单

- **声部**：`laozi` / `lunyu` / `tanjing` / `mao` / `kapital` / `chuanxi` / `zhuangzi` / `stoic` / `frankl` / `zhuan-yi`
- **困境**：`意义与方向危机` / `工作倦怠与异化` / `焦虑与精神内耗` / `人际与情感困扰` / `欲望与物质匮乏感`

### 禁用场景的写法

禁用场景是语料质量的关键。好的禁用场景要具体说明"什么情况下不该用这条"：

- ✅ 好：`"刚失业/破产等真实资源危机——此时不宜讲知足，应先处理现实问题"`
- ✅ 好：`"焦虑型内耗者——须先处理情绪再谈战略"`
- ❌ 差：`"不适用"`（没说明为什么不适用）
- ❌ 差：留空（校验会报错）

---

## 概念贡献规范

每条概念加入对应声部文件（如 `concepts/laozi.json`）：

```json
{
  "id": "concept-laozi-011",
  "name": "无为",
  "声部": "laozi",
  "定义": "不逆势硬来，反而什么都做得成。不是躺平，是不跟规律较劲。",
  "关联概念": ["concept-laozi-001"],
  "关联语料": ["laozi-007"],
  "适用场景": ["焦虑与精神内耗", "工作倦怠与异化"],
  "禁用场景": ["需要果断行动的紧急情况——此时'无为'会被误读为拖延"]
}
```

`关联语料` 里的 id 必须在 corpus/ 中真实存在（校验脚本会检查悬空引用）。

---

## 提交前必做的检查

### 1. 语料/概念校验（改了 corpus/ 或 concepts/ 后必跑）

```powershell
node tests/validate-corpus.mjs corpus concepts
```

会检查：字段完整性、id 唯一性、声部合法性、困境类型合法性、概念悬空引用。**全部通过才能提交。**

### 2. CLI 测试（改了 cli/ 后必跑）

```powershell
cd cli
dart analyze   # 静态检查零问题
dart test      # 单元测试全过
```

### 3. 一致性自查

如果改动涉及以下任一，需联动多处保持一致：

| 改动 | 需同步的位置 |
|---|---|
| 解药矩阵（新增/调整声部映射） | 设计文档 §4 + 角色卡 §9 + paperkit/matrix-cheatsheet.md + cli/lib/rag/vector_search.dart 的 `kDilemmaVoiceBonus` |
| 角色卡内容 | prompts/mingtian-vX.Y.md 与设计文档 §9 必须 diff 一致；升版本号；§12 登记 |
| 安全条款 | 角色卡 + paperkit + cli 危险词表 + 设计文档 §边界 |

---

## 提交流程

1. Fork 仓库 → 建分支（`feat/新增老子语料` / `fix/cli-检索bug` 等）
2. 改动 → 跑上述检查
3. Commit 信息用中文，说明改了什么、为什么。例：`feat: 新增 laozi-052~055（千里之行/慎终如始等 4 条）`
4. 提交 Pull Request，在描述里说明：改了什么、跑了哪些检查、结果如何

---

## 安全相关的特别约定

明镜面对的是精神困境，部分场景涉及安全（自伤、家暴、严重心理疾病）。贡献时注意：

- **不要**贡献可能在家暴/侵害场景被误用的"忍让/反省"类语料，除非禁用场景明确写出"涉及真实侵害禁用"
- **不要**在脆弱态（刚失恋/崩溃）场景示范"反求诸己/内自省"——这会把伤害归咎受害者
- **不要**移除或弱化任何安全条款（角色卡边界、CLI 危险词表、paperkit 安全筛查）

如果你不确定某条贡献是否安全，在 PR 描述里标注"需安全审查"，维护者会重点 review。

---

## 毛泽东声部的特别约定

毛泽东声部**仅限哲学方法论**（矛盾论/实践论/调查研究/持久战/战略藐视战术重视等），**严禁**涉及任何政治立场、历史事件评价、人物功过评判。这是项目的硬边界，详见设计文档 §7。

贡献该声部语料时，白话解析必须是生活化转译（如"抓主要矛盾"→"一堆麻烦里找主因"），不得带入政治语境。

---

## 问题与讨论

- 发现 bug 或有改进想法 → 提 Issue
- 不确定某条贡献是否合适 → 先开 Issue 讨论，再动手
- 设计层面的重大改动（如调整矩阵、新增声部）→ 务必先讨论达成共识

期待你的贡献。明镜因更多人参与而更完整。
